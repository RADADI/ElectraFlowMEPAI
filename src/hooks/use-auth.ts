// Convenience re-export so components can import from either location.
export { useAuth, getStoredRole, setStoredRole, clearStoredRole } from "@/contexts/auth-context";
export type { AuthState } from "@/contexts/auth-context";
