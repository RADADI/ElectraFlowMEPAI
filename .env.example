# ─── Rename this file to .env and fill in your values ────────────────────────
# The app works without any keys — it uses mock/demo data by default.

# ─── Clerk Authentication (optional) ─────────────────────────────────────────
# If absent, the app uses localStorage-based mock authentication.
# 1. Create a free account at https://clerk.com
# 2. Create a new application
# 3. Copy your Publishable Key from Clerk Dashboard → API Keys
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ─── Supabase Backend ─────────────────────────────────────────────────────────
# If absent, the app uses demo/mock data. No crash, no errors.
# 1. Create a free Supabase project at https://supabase.com
# 2. Go to Project Settings → API
# 3. Copy Project URL and anon/public key below
# 4. Run src/database/schema.sql, then rls-policies.sql, then seed.sql
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxxxxxxxxx

# ─── Supabase Organization ID (Phase 4) ──────────────────────────────────────
# Required for real CRUD when Supabase is configured.
# Find your org UUID after running seed.sql:
#   SELECT id FROM organizations LIMIT 1;
# This value is cached in localStorage after first successful profile lookup,
# so you can omit this once the cache is warm — but it's safer to set it.
VITE_SUPABASE_ORG_ID=

# ─── Supabase Service Role Key (Phase 4 — DEV ONLY) ──────────────────────────
# Required for Phase 4 CRUD (create/update/archive projects) before Clerk JWT
# integration (Phase 5). This key BYPASSES Row Level Security.
#
# ⚠️  SECURITY WARNING:
#   - NEVER commit this key to git.
#   - NEVER deploy with this key in a public environment.
#   - Copy to .env.local (not .env) so it is excluded from version control.
#   - Phase 5 replaces this with proper Clerk JWT → Supabase auth wiring.
#
# Find this key in: Supabase Dashboard → Project Settings → API → service_role key
VITE_SUPABASE_SERVICE_ROLE_KEY=
